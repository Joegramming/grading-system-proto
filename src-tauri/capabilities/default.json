{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the main window. The app's own load_gradebook / save_gradebook commands are not gated here.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
