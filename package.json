{
  "name": "gradebook",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Multi-section gradebook — students, weighted categories, assignments, and printable reports.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "tauri": "tauri",
    "app:dev": "tauri dev",
    "app:build": "tauri build",
    "kill-port": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/kill-port.ps1",
    "predeploy": "vite build --mode pages",
    "deploy": "gh-pages -d dist --dotfiles"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4",
    "gh-pages": "^6.3.0",
    "vite": "^7.1.5",
    "vitest": "^4.1.11"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.1",
    "docx": "^9.7.1",
    "exceljs": "^4.4.0"
  }
}
