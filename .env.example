# Vite only exposes vars prefixed with VITE_. Copy to .env.local to use.
#
# The app needs no configuration: storage is chosen automatically —
# a JSON file in the OS app-data folder when running as the Tauri desktop
# app, localStorage when running in a plain browser.
