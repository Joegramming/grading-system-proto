node_modules/
dist/
.vite/

# local reference material — client templates + scratch notes, not part of the app
src/docs/

# local env overrides — .env.example is committed, real values are not
.env
.env.local
.env.*.local

*.log
npm-debug.log*

.DS_Store
Thumbs.db
